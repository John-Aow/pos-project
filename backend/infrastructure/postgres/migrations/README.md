# Migrations

Place new SQL migration files in this directory using a monotonic naming scheme.

Guidelines:
- Never edit a migration after it has been applied.
- Keep schema changes in new files only.
- Use down migrations only when the broader release process requires them.
